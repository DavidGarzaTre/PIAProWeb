@model IEnumerable<PIAProWeb.Models.dbModels.Producto>

@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Index";
}

<h1>Administración de Productos</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Create New</a>

</p>
<table class="table" id="myTable">
    <thead>
        <tr>
            <th>
                @*@Html.DisplayNameFor(model => model.NombreProducto)*@ Nombre
            </th>
            <th>
               @* @Html.DisplayNameFor(model => model.PrecioProducto)*@ Precio
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.ImagenProducto)*@ Imagen
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.StockProducto)*@ Stock
            </th>

            <th>
                @*@Html.DisplayNameFor(model => model.IdCategoriaNavigation)*@
                Categoria
            </th>
            <th>
                Editar
            </th>
            <th>
                Detalles
            </th>
            <th>
                Borrar
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreProducto)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrecioProducto)
            </td>

            <td>
                    <img src="~/Fotos/Productos/@Html.DisplayFor(modelItem => item.ImagenProducto)" width="300" height="150"/>
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StockProducto)
                
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdCategoriaNavigation.Nombre)
            </td>
            <td>
                <a class="btn btn-warning" asp-action="Edit" asp-route-id="@item.IdProducto">Edit</a> 
            </td>
                <td>
                    <a class="btn btn-primary" asp-action="Details" asp-route-id="@item.IdProducto">Details</a> 
                    
                </td>
                <td>
                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.IdProducto">Delete</a>
                </td>
        </tr>
}
    </tbody>
</table>
